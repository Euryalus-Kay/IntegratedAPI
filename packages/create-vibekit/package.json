{
  "name": "create-vibekit",
  "version": "0.1.0",
  "description": "Create a new app with VibeKit backend in one command.",
  "type": "module",
  "bin": {
    "create-vibekit": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "dev": "tsup src/index.ts --format esm --watch"
  },
  "dependencies": {
    "prompts": "^2.4.0",
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "@types/prompts": "^2.4.0",
    "tsup": "^8.0.0",
    "typescript": "^5.4.0"
  }
}

{
  "name": "@vibekit/server",
  "version": "0.1.0",
  "description": "VibeKit API backend server [FUTURE]",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "dev": "tsup src/index.ts --format esm --watch"
  },
  "dependencies": {
    "vibekit": "workspace:*",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.4.0"
  }
}
